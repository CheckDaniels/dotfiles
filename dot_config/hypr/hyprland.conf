# This is an example Hyprland config file.
#
# Refer to the wiki for more information.

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#
# info at https://wiki.hyprland.org/
# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto

# mirror two screens HDMI-A-1 to left of HDMI-A-2
# monitor=HDMI-A-1,1920x1080@60,0x0,1,mirror,HDMI-A-2
# monitor=,preferred,auto,1,mirror,HDMI-A-1


#~~~~~~~~~~#
# MONITORS #
#~~~~~~~~~~#

# Configure your Display resolution, offset, scale and Monitors here, use `hyprctl monitors` to get the info.
monitor=HDMI-A-1,1920x1080@60,0x0,1
monitor=,preferred,auto,1,mirror,eDP-1
#workspace=HDMI-A-1,1
#monitor=HDMI-A-2,1920x1080@60,1920x0,1
#workspace=HDMI-A-2,2

# Example :
#monitor=eDP-1,1920x1080@60,0x0,1
#monitor=eDP-1,transform,0
#monitor=eDP-1,addreserved,10,10,10,10
#workspace=eDP-1,1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more


#~~~~~~~~~~~#
# VARIABLES #
#~~~~~~~~~~~#

$mainMod = SUPER
$localBin = $HOME/.local/bin
$scriptsDir = $HOME/.config/hypr/scripts
$hyprDir = $HOME/.config/hypr
$term = $HOME/bin/alacritty/tmux.sh
$menu = $scriptsDir/menu
$fullmenu = $scriptsDir/fullmenu
$volume = $scriptsDir/volume
$backlight = $scriptsDir/brightness
$screenshot = $scriptsDir/screenshot
$lock = $scriptsDir/lockscreen
$colorpicker = $scriptsDir/colorpicker
$wofi_beats = $scriptsDir/wofi-beats
$files = $HOME/bin/ranger/ranger.sh
$browser = brave
$editor = xed


#~~~~~~~~~~~~~~~~~~~~~~~#
# ENVIRONMENT VARIABLES #
#~~~~~~~~~~~~~~~~~~~~~~~#

env = XCURSOR_SIZE,24
env = QT_QPA_PLATFORM,wayland
env = GDK_BACKEND,wayland
env = CLUTTER_BACKEND,wayland
env = SDL_VIDEODRIVER,wayland
env = HYPRSHOT_DIR,Pictures/Screenshots
#env = XDG_CURRENT_DESKTOP='X-Generic'


#~~~~~~~~~~~~~~~~~~~~~~#
# STARTUP APPLICATIONS #
#~~~~~~~~~~~~~~~~~~~~~~#

exec-once = $scriptsDir/startup
# exec-once = swaybg -m fill -i $HOME/.config/hypr/bg/att-plain-04.png
exec-once = swaybg -m fill -i /usr/share/backgrounds/nordic-wallpapers/nord_buildings.png
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = $scriptsDir/portal
exec-once = hypridle
exec-once = ulauncher --hide-window --no-window-shadow
exec-once = nm-applet
exec-once = sleep 3; nextcloud --background 
exec-once = copyq --start-server
exec-once = udiskie
exec-once = ~/bin/hyprland/maximized_win.sh
exec-once = sleep 2; morgen
# exec-once = /usr/bin/variety --profile /home/daniel/.config/variety/
# exec-once = stretchly
# exec-once = sleep 2; mailspring --password-store="gnome-libsecret" --background

# other clipboard-managers
#exec-once = wl-paste -p -t text --watch clipman store -P --histpath="~/.local/share/clipman-primary.json"
#exec-once = wl-paste --type text --watch cliphist store 
##Stores only text data
#exec-once = wl-paste --type image --watch cliphist store 
##Stores only image data

# with lock - hyprctl
# exec-once = swayidle -w timeout 1200 '$lock' timeout 1200 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on' before-sleep '$lock'

# with lock - swaylock
# exec-once = swayidle -w timeout 1200 'swaylock -f -c 111111' timeout 1200 '' resume '' before-sleep 'swaylock -f -c 111111'

# without lock
# exec-once = swayidle -w  timeout 1200 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on'


#~~~~~~~~~~~~~~~~~~#
# GENERAL SETTINGS #
#~~~~~~~~~~~~~~~~~~#

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
  # qwerty
  kb_layout = de

  # azerty
  #kb_layout = be

  kb_variant =
  kb_model =
  kb_options =
  kb_rules =

  follow_mouse = 1
  numlock_by_default = 0

  touchpad {
    natural_scroll = true
    tap-to-click = true
    drag_lock = true
    disable_while_typing = true
  }

  #sensitivity = 0.0 # -1.0 - 1.0, 0 means no modification
}

misc {
  disable_hyprland_logo = true
  disable_splash_rendering = true
  mouse_move_enables_dpms = true
  no_direct_scanout = true #for fullscreen games
  vfr = true
}

binds {
  workspace_back_and_forth = true
}

general {
  # See https://wiki.hyprland.org/Configuring/Variables/ for more

  gaps_in = 3
  gaps_out = 7
  border_size = 2

  #one color
  col.active_border = rgba(5294e2aa)

  #two colors - gradient

  # col.active_border = rgba(7aa2f7aa) rgba(c4a7e7aa) 45deg
  col.inactive_border = rgba(414868aa)

  layout = master
  # layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 4

    blur {
        enabled = false
        # size = 3
        # passes = 1
    }
    # fullscreen_opacity = 0.9

    drop_shadow = false
    # shadow_range = 4
    # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)
}

blurls = waybar

animations {
  enabled = true

  # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

  bezier = myBezier, 0.05, 0.9, 0.1, 1.05

  animation = windows, 1, 7, myBezier
  animation = windowsOut, 0, 7, default, popin 80%
  animation = fade, 0, 7, default
  animation = border, 0 , 10, default
  animation = workspaces, 1, 6, default
}

dwindle {
  # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
  pseudotile = true
  preserve_split = true
}

master {
  # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
  #new_is_master = true
  new_status = master
  mfact = 0.5
}

gestures {
  # See https://wiki.hyprland.org/Configuring/Variables/ for more
  workspace_swipe = true
  workspace_swipe_fingers = 3
}

cursor {
  # inactive_timeout = 10
  # enable_hyprcursor = true
  hide_on_key_press = true
}


#~~~~~~~~~~~~~~~~~~~~#
# CUSTOM WINDOWRULES #
#~~~~~~~~~~~~~~~~~~~~#

# Example windowrule v2
windowrulev2 = tile, class:^(Spotify)$
# ulauncher
windowrulev2 = stayfocused, class:^(ulauncher)$
windowrulev2 = pin, class:^(ulauncher)$
#xfce4-appfinder
windowrulev2 = float, class:^(xfce4-appfinder)$
windowrulev2 = move 10 50, class:^(xfce4-appfinder)$
# nextcloud
windowrulev2 = float, title:^(Nextcloud)$
windowrulev2 = move 1450 50, title:^(Nextcloud)$
windowrulev2 = noinitialfocus, title:^(Nextcloud)$
windowrulev2 = pin, title:^(Nextcloud)$
# Bitwarden
windowrulev2 = float, class:^(Bitwarden)$
windowrulev2 = move 1000 50, class:^(Bitwarden)$
windowrulev2 = size 900 800, class:^(Bitwarden)$

# scid
windowrulev2 = float, title:^(Variations)$
windowrulev2 = stayfocused, title:^(Variations)$

# copyq
windowrulev2 = float, class:^(com.github.hluk.copyq)$
windowrulev2 = pin, class:^(com.github.hluk.copyq)$
windowrulev2 = move 1110 50, class:^(com.github.hluk.copyq)$
windowrulev2 = size 800 500, class:^(com.github.hluk.copyq)$

#~~~~~~~~~~~~~~~~~~~~~~~~#
# CUSTOM WORKSPACE-RULES #
#~~~~~~~~~~~~~~~~~~~~~~~~#

# brave
# windowrulev2 = workspaces 1, class:^(Brave-browser)$
# alacritty-tmux
# windowrulev2 = workspaces 8, class:^(Alacritty)$

# obsidian
windowrulev2 = workspaces 2, class:^(obsidian)$
# thunar
windowrulev2 = workspaces 3, class:^(thunar)$
# copilot
windowrulev2 = workspaces 4, class:^(Microsoft-edge)$
# ChatGPT
windowrulev2 = workspaces 4, class:^(WebApp-ChatGPT9694)$
# Superproductivity
windowrulev2 = workspaces 5, class:^(superProductivity)$
# Morgen Calendar
windowrulev2 = workspaces 5, class:^(Morgen)$
# Mailspring
windowrulev2 = workspace 6, class:^(Mailspring)$
# Teams
windowrulev2 = workspace 7, class:^(teams-for-linux)$
# lichess
windowrulev2 = workspace 9, class:^(WebApp-lichess6472)$
# scid
windowrulev2 = workspace 9, class:^(Start.tcl)$

# windowrulev2 = workspace 4, class:^(Apache Directory Studio)$
# windowrulev2 = bordercolor rgb(EE4B55) rgb(880808), fullscreen:1
# windowrulev2 = bordercolor rgb(282737) rgb(1E1D2D), floating:1
# windowrulev2 = opacity 0.8 0.8, pinned:1
# windowrulev2 = workspace 10, class:^(Microsoft-edge)$
# windowrulev2 = workspace 8 silent, class^(Steam)$, title:^(Steam)$
# windowrulev2 = float,class:^(firefox)$,title:^(Picture-in-Picture)$

# for waybar yay update click
windowrulev2 = animation popin, class:^(update)$, title:^(update)$
windowrulev2 = float, class:^(update)$, title:^(update)$
windowrulev2 = size 60% 50%, class:^(update)$, title:^(update)$
windowrulev2 = center, class:^(update)$, title:^(update)$

# for ttyclock
windowrulev2 = float, class:^(clock)$, title:^(clock)$
windowrulev2 = size 33% 27%, class:^(clock)$, title:^(clock)$
windowrulev2 = center, class:^(clock)$, title:^(clock)$


#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#
# TOGGLE-KEYBINDINGS FOR GAMING #
#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#

# ulauncher
bind = CTRL, SPACE,exec, ulauncher-toggle
# toggle Superproductivity
bind = ALT, S, exec, xdotool key "alt+s"


#~~~~~~~~~~~~~~~~~~~~#
# CUSTOM KEYBINDINGS #
#~~~~~~~~~~~~~~~~~~~~#

# frequently used keybindings
bind = $mainMod SHIFT, R, exec, hyprctl reload
bind = $mainMod SHIFT, Space, togglefloating
bind = $mainMod, F, fullscreen
bind = $mainMod, Q, killactive
bind = $mainMod, Return, exec, $term


# system-menu
bind = CTRL ALT, M, exec, nwg-menu -cmd-lock 'hyprlock' -cmd-logout 'hyprctl dispatch exit' -va top -wm 'hyprland'
# menu
bind = , menu, exec, $menu
bind = SHIFT, menu, exec, $fullmenu
# internet browser
bind = $mainMod, I, exec, $browser
# file explorer
bind = $mainMod, E, exec, $files
# close window
bind = CTRL ALT, X, killactive
# fullscreen
bind = $mainMod, Z, fullscreen, 1
# hide active window
bind = $mainMod,D,exec, ~/bin/hyprland/hide_unhide_window.sh h
# show hide window 
bind = $mainMod SHIFT,D,exec, ~/bin/hyprland/hide_unhide_window.sh s
# toggle calendar
bind = $mainMod, C, exec, morgen
# auto-cpufreq
bind = $mainMod, minus, exec, alacritty -e sudo auto-cpufreq --force=powersave
bind = $mainMod, numbersign, exec, alacritty -e sudo auto-cpufreq --force=reset
bind = $mainMod, plus, exec, alacritty -e sudo auto-cpufreq --force=performance
# QOwnNotes
bind = $mainMod, N, exec, QOwnNotes #bind = $mainMod, N, exec, marktext ~/Nextcloud/Notes/
# Markdown editor
bind = $mainMod, M, exec, marktext
# ChatGPT
bind = $mainMod, A, exec, sh -c 'XAPP_FORCE_GTKWINDOW_ICON="/home/daniel/Pictures/Icons/ChatGPT_logo.svg" librewolf --class WebApp-ChatGPT9694 --name WebApp-ChatGPT9694 --profile /home/daniel/.local/share/ice/firefox/ChatGPT9694 --no-remote "https://chatgpt.com/?oai-dm=1"'
# Copilot
bind = $mainMod, B, exec, microsoft-edge-stable https://www.bing.com/chat
# reMarkable-cloud
bind = $mainMod, R, exec, sh -c 'XAPP_FORCE_GTKWINDOW_ICON="tablet" librewolf --class WebApp-reMarkable6391 --name WebApp-reMarkable6391 --profile /home/daniel/.local/share/ice/firefox/reMarkable6391 --no-remote "https://my.remarkable.com/myfiles"'
# Newsflash
bind = $mainMod SHIFT, N, exec, newsflash
# reload waybar
bind = $mainMod CTRL, W, exec, killall waybar; ~/.config/hypr/scripts/statusbar
# pavucontrol
bind = $mainMod, V, exec, pavucontrol


# other applications
bind = CTRL ALT, A, exec, xfce4-appfinder
bind = CTRL ALT, F, exec, thunar
bind = CTRL ALT, E, exec, archlinux-tweak-tool
bind = CTRL ALT, L, exec, librewolf
bind = CTRL ALT, G, exec, chromium -no-default-browser-check
bind = CTRL ALT, NEXT, exec, conky-rotate -n
bind = CTRL ALT, P, exec, pamac-manager
bind = CTRL ALT, PREVIOUS, exec, conky-rotate -p
bind = CTRL ALT, R, exec, rofi-theme-selector
bind = CTRL ALT, Return, exec, foot
bind = CTRL ALT, S, exec, spotify
bind = CTRL ALT, T, exec, superproductivity
bind = CTRL ALT, U, exec, pavucontrol
bind = CTRL ALT, V, exec, vivaldi-stable
bind = CTRL ALT, W, exec, arcolinux-welcome-app
bind = CTRL ALT, END, exec, alacritty --class btop -T btop -e btop


# change wallpaper
# bind = ALT, n, exec, $scriptsDir/changeWallpaper
# bind = ALT, p, exec, $scriptsDir/changeWallpaper
# bind = ALT, left, exec, $scriptsDir/changeWallpaper
# bind = ALT, right, exec, $scriptsDir/changeWallpaper
# bind = ALT, up, exec, $scriptsDir/changeWallpaperDesktop
# bind = ALT, down, exec, $scriptsDir/changeWallpaperDesktop


bind = $mainMod SHIFT, M, exec, hyprctl dispatch splitratio -0.1
bind = $mainMod, M, exec, hyprctl dispatch splitratio 0.1

bind = $mainMod SHIFT, Y, exec, alacritty --class clock -T clock -e tty-clock -c -C 7 -r -s -f "%A, %B, %d"
bind = $mainMod SHIFT, I, layoutmsg, addmaster
bind = $mainMod, J, layoutmsg, cyclenext
bind = $mainMod, K, layoutmsg, cycleprev

bind = $mainMod CTRL, Return, layoutmsg, swapwithmaster
bind = $mainMod, Space, exec, $scriptsDir/changeLayout
bind = $mainMod, X, exec, archlinux-logout
bind = $mainMod, Escape, exec, archlinux-logout
bind = $mainMod, Y, exec, alacritty --class update -T update -e cava # f to cycle through foreground colors

# Mainmod + Function keys
bind = $mainMod, F1, exec, $browser
bind = $mainMod, F2, exec, $editor
bind = $mainMod, F3, exec, inkscape
bind = $mainMod, F4, exec, gimp
bind = $mainMod, F5, exec, meld
bind = $mainMod, F6, exec, vlc
bind = $mainMod, F7, exec, virtualbox
bind = $mainMod, F8, exec, $files
bind = $mainMod, F9, exec, lollypop
bind = $mainMod, F10, exec, spotify

# Special Keys
bind = , xf86audioraisevolume, exec, $volume --inc
bind = , xf86audiolowervolume, exec, $volume --dec
bind = , xf86audiomute, exec, $volume --toggle
# bind = , xf86audioplay, exec, playerctl play-pause
# bind = , xf86audionext, exec, playerctl next
# bind = , xf86audioprev, exec, playerctl previous
# bind = , xf86audiostop, exec, playerctl stop
bind = , xf86monbrightnessup, exec, $backlight --inc
bind = , xf86monbrightnessdown, exec, $backlight --dec

# Backlight control
bind = $mainMod SHIFT, equal, submap, backlight
submap = backlight
bind = , escape, submap, reset
submap = reset

# Volume control
bind = $mainMod, equal, submap, volume
submap = volume
bind = , equal, exec, $volume --inc
bind = , minus, exec, $volume --dec
bind = , 0, exec, $volume --toggle
bind = , 9, exec, $volume --toggle-mic
bind = , escape, submap, reset
submap = reset

# Resize
# binde = $mainMod SHIFT, H, resizeactive,-50 0
# binde = $mainMod SHIFT, L, resizeactive,50 0
# binde = $mainMod SHIFT, K, resizeactive,0 -50
# binde = $mainMod SHIFT, J, resizeactive,0 50

# Move
bind = $mainMod ALT, H, movewindow, l
bind = $mainMod ALT, L, movewindow, r
bind = $mainMod ALT, K, movewindow, u
bind = $mainMod ALT, J, movewindow, d

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Special workspace
bind = $mainMod SHIFT, U, movetoworkspace, special
bind = $mainMod, U, togglespecialworkspace,

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = $mainMod, G, togglegroup
bind = $mainMod, tab, workspace, m+1
bind = $mainMod SHIFT, tab, workspace, m-1
bind = ALT, tab, workspace, previous
bind = ALT SHIFT, tab, workspace, r+1

bind = $mainMod, O, exec, $colorpicker
bind = $mainMod SHIFT, O, exec, $term --class hyprpicker --hold -e hyprpicker
bind = $mainMod CTRL, S, exec, $wofi_beats



#~~~~~~~~~~~~~~~~~~~~#
# SCREENSHOT-UTILITY #
#~~~~~~~~~~~~~~~~~~~~#

# Screenshot a window
bind = $mainMod, PRINT, exec, hyprshot -m window
# Screenshot a monitor
bind = , PRINT, exec, hyprshot -m output
# Screenshot a region
bind = $shiftMod, PRINT, exec, hyprshot -m region

# # Screenshot keybindings
# bind = , Print, exec, $screenshot --now
# bind = $mainMod, Print, exec, $screenshot --in5
# bind = SHIFT, Print, exec, $screenshot --in10
# bind = CTRL, Print, exec, $screenshot --win
# bind = $mainMod CTRL, Print, exec, flameshot gui


#~~~~~~~~~~~~~~~~~~~~~~~#
# WORKSPACE-KEYBINDINGS #
#~~~~~~~~~~~~~~~~~~~~~~~#

# Qwerty
# workspace navigation
bind = $mainMod, period, workspace, +1
bind = $mainMod, comma, workspace, -1
bind = $mainMod CTRL, period, movetoworkspace, +1
bind = $mainMod CTRL, comma, movetoworkspace, -1
bind = $mainMod SHIFT, period, movetoworkspacesilent, +1
bind = $mainMod SHIFT, comma, movetoworkspacesilent, -1
bind = $mainMod, l, workspace, e+1
bind = $mainMod, h, workspace, e-1
bind = $mainMod CTRL, l, movetoworkspace, e+1
bind = $mainMod CTRL, h, movetoworkspace, e-1
bind = $mainMod SHIFT, l, movetoworkspacesilent, e+1
bind = $mainMod SHIFT, h, movetoworkspacesilent, e-1

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, +1
bind = $mainMod, mouse_up, workspace, -1


# Qwerty
# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10


# Qwerty
# Move active window and follow to workspace
bind = $mainMod CTRL, 1, movetoworkspace, 1
bind = $mainMod CTRL, 2, movetoworkspace, 2
bind = $mainMod CTRL, 3, movetoworkspace, 3
bind = $mainMod CTRL, 4, movetoworkspace, 4
bind = $mainMod CTRL, 5, movetoworkspace, 5
bind = $mainMod CTRL, 6, movetoworkspace, 6
bind = $mainMod CTRL, 7, movetoworkspace, 7
bind = $mainMod CTRL, 8, movetoworkspace, 8
bind = $mainMod CTRL, 9, movetoworkspace, 9
bind = $mainMod CTRL, 0, movetoworkspace, 10
bind = $mainMod CTRL, bracketleft, movetoworkspace, -1
bind = $mainMod CTRL, bracketright, movetoworkspace, +1

# Qwerty
# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10
bind = $mainMod SHIFT, bracketleft, movetoworkspacesilent, -1
bind = $mainMod SHIFT, bracketright, movetoworkspacesilent, +1
